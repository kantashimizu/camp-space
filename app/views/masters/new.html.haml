= form_for(@master, url: masters_path,method:'POST' ) do |f|
  = f.hidden_field :area_id ,value:@area.id
  = f.hidden_field :item_id ,value:@area.id
  = f.hidden_field :user_id ,value:current_user.id
  = f.submit "カートに入れる"